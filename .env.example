# This is a template for project configuration.
# Copy this file to .env and fill in your actual values.

# --- MinIO Configuration ---
# Use strong, unique passwords in your actual .env file
MINIO_ACCESS_KEY=changeme
MINIO_SECRET_KEY=changeme_strong_password
MINIO_DEFAULT_BUCKET=raw-data

# --- PostgreSQL Configuration ---
PG_USER=changeme
PG_PASSWORD=changeme_strong_password
PG_DATABASE=metadata_db

